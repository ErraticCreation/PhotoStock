<% if notice %>
    <p class="text-center notice alert alert-info"><%= notice %></p>
<% elsif alert %>
    <p class="text-center notice alert alert-danger"><%= alert %></p>
<% end %>
<% flash.each do |name, msg| %>
    <%= content_tag :div, msg, class: name %>
<% end %>